import { ButtonDirectiveDirective } from './button.directive.directive';

describe('ButtonDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
